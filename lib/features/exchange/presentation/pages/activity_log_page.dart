import 'package:flutter/material.dart';

class ActivityLogPage extends StatelessWidget {
  // Tabs: طلباتي / خدماتي
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Activity Log')),
      body: Center(child: Text('Activity Log Page')),
    );
  }
}
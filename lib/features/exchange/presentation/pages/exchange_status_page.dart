import 'package:flutter/material.dart';

class ExchangeStatusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Exchange Status')),
      body: Center(child: Text('Exchange Status Page')),
    );
  }
}
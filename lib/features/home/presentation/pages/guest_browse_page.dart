import 'package:flutter/material.dart';

class GuestBrowsePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Guest Browse')),
      body: Center(child: Text('Guest Browse Page')),
    );
  }
}